#include<bits/stdc++.h>
using namespace std;

int main()
{
    int N;
    
    cin >> N;
    while(N--){
        long long n,x,y;
        cin >> n >> x >> y;
        if(y*2+x<=n){
            cout<<(y+x)*(y+x+1)/2<<endl;
            continue;
        }
        if(y>=n){
            cout<<(n+x+1)*(n+x+1)/4<<endl;
            continue;
        }
        cout <<((n+y)*(n+y)-2*(x-n)*(x-n))/4<< endl;

    }


    return 0;
}

###
编写C++代码计算最大伤害值的任务。根据以下规则一步步执行：
1. 代码需处理多组测试数据，第一行输入的整数 `T` 代表数据组数。
2. 对于每组测试数据，需读取三个正整数 `n`、`x`、`y`，分别代表回合数、初始攻击力、初始磨刀石数量。
3. 需根据游戏规则（每回合先是磨刀(一次)阶段和后是攻击(一次)阶段，同一回合可同时磨刀和攻击，磨刀可提升攻击力，且在磨刀石用完前每回合都会磨刀，攻击会造成等同于刀攻击力的伤害并使刀攻击力减一，刀攻击力归零会损坏不能再攻击）计算最大造成的伤害。
4. 伤害最大的情况一定是（在磨刀次数用完前一直使用，在合适的回合开始攻击且一直攻击到结束）。
你只用简化为计算何时开始攻击总攻击最高

请回答问题：
输入：测试数据组数：`T`，每组测试数据中的回合数：`n`，初始攻击力：`x`，初始磨刀石数量：`y`
输出：对于每一组测试数据，输出一行一个整数，代表最大造成的伤害

要求：
1 输出为符合C++ 语言规范的代码
2 代码需正确计算并输出每一组测试数据对应的最大伤害值
###